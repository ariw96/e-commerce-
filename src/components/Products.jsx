import React from 'react';
import { Grid } from '@material-ui/core';
const products = [
    {id: 1, name: 'hat', descreption: 'clothing'},
    {id: 2, name: 'camera', descreption: 'electronic'},
];

const Products = () => {
    <main>
        <Grid container justify='center' spacing={4}></Grid>

    </main>
    }
    export default Products;